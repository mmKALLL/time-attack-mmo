
Greetings!

I want to make a single-player desktop RPG in an old-school MMO style, with the following features:

- resolution 1080p
- possibility to extend into an MMO later
- grid map with 64x64 2d sprites; enemies can never overlap but players can pass each other
- there are many layers of classes, which can be "mixed" into each other (e.g. you can advance beginner -> archer -> ranger + beginner -> magician -> fire wizard => flame ranger -> high flame ranger, or beginner -> swordsman -> crusader (tank) + beginner -> thief -> shadower => nimble knight -> high nimble knight)
- characters generally have 2 or 3 frame animations
- movement happens with the arrow keys, you always move 1 cell whenever a key is pressed; there is no speed limit
- whenever you touch an enemy combat starts (without a screen transition). In combat, the enemy is "stuck" to the character, so if you touch an enemy on the left, you become a 2x1 block and move the enemy with you. You can stick even more enemies to the block, creating fights of arbitrary size and shape
- while in combat, both sides use their active skill automatically every 1.5 seconds, with a square-shaped timer counting down to the next skill use
- some skills have a number of uses before they go on cooldown; some cooldowns are passive and others require the skill to be actively selected
- some skills have different attack shapes; e.g. you can have a 3x1 attack that hits enemies that are stuck to you in a row
- other players (and allies) can attack the same enemies by choosing to stick into your combat as well
- you can press a skill hotkey (default 1-9 keys) to change the active skill
- the stat distribution of the enemies should be symmetric to players; a level 25 player fighting a level 25 enemy should have a roughly equal chance of win and loss, leading to challenging roguelite-like gameplay. However, the focus is on making grinding and questing very fun; typically a player would hunt enemies roughly 5 levels below them

I am asking Claude Code for help with designing the rough framework.

Screen ideas:

- world map
- dungeon gameplay with 3 players in the party
- shop
- skill / attribute allocation
- character creation
- hotkey configuration
- NPC chat with quest

I'd like to make a TS+React frontend-only game prototype of this. Could you set up a scaffold that we can use to set things up once Claude Design finishes its work?

----

On the second job layer, each base class should have 3 second classes with different MMO roles. Each second class can combine with any other second class; each of the second classes have 2-3 guaranteed skills and each pair of second classes have 2-3 specialized skills. So every merged class has 6-9 skills total. Similar effect for the "high" variation, which will maybe be added later. The base classes have in general these flavors, but the second classes tend to shift into different MMO roles.

Swordsman - tanky and easy-to-play melee fighter; has 1v1 and adjacent 4/8 tile attacks. Second classes: Knight (easy tank), Paladin (mild healing and more area/cooldown attacks), Duelist (more technical, stronger 1v1 DPS, some weaker ranged/debuffing attacks)

Archer - nimble 1v1 attacker; can start fights at range (enemies have "invisible" slots inbetween themselves and the player that are collision-detected against walls but not against other enemies; they need to spend initial turns closing in). Two archetypes: hunter: fast attack speed with high miss rate, sniper: slow attack speed with accurate critical hits. Mixed "ranger" second class that is more complex and suited to nature/buffs/arcane than DPS. Attacks are almost always in a straight line: never diagonally, in multiple directions, or in large areas

Magician - slow mob attacker; large directional hitboxes, lots of cooldowns to manage. Second classes are arcane mage (like Gandalf / D&D wizard, buffers / debuffers), fire wizard, druid (Diablo 2 feeling huge slow attacks + healer + buffs)

Rogue - very technical glass cannon type character; mixture of close and far range attacks. Can stack multiple thief attacks each round: 2 in first class and 3 from second class onwards. Second classes: assassin (a bit less technical with improved critical hits, poison, strong against enemies with status effects); shadower (very high dodge, melee class with good solo ability); ninja (throwing stars and other ranged weapons, traps, debilitation, etc)

----

Status effects should in general be very debilitating, e.g. poison dealing 10% max hp damage each round. Characters with multiple status effects have an increasing incoming critical hit rate.

Enemy AoE attacks should be telegraphed 1-3 attack rounds ahead of time. Otherwise enemies always attack the initially attacked nearest player in range (from up to 8 adjacent directions). However, player movement is not limited to the attack rounds, so they can dodge quite well - as long as they don't have too many enemies with them.

----

Stat attributes should probably be something like STR / DEX / VIT / INT, with some derived stats like HP, MP, min/max damage, defense, critical hit chance, dodge chance, etc. However, each class should be able to get meaningful value from all of the stats.

Attacks and skills have a physical component and a magical component. The phys-mag ratio between these components is based on class: 

Beginner / leader: 50% physical, 50% magical
Fighter: 80% physical, 20% magical
Archer: 40% physical, 60% magical
Magician: 20% physical, 80% magical
Rogue: 60% physical, 40% magical

Maximum damage is calculated with:

Physical = Str * 4 + Dex * 2 + Vit * 1
Magical = Int * 4 + Dex * 2 + Vit * 1

Minimum damage is 60% of maximum, except for rogues who have 40% of maximum.

HP = Vit * 30 + Str * 5 + Level * 5
MP = Int * 10 + Dex * 2 + Level * 2
Def = Vit * 2 + Str * 1
Crit = (Dex * 2 + Int * 1) ^ 0.7 until (Dex * 2 + Int * 1) < 300, then ^0.6 for the exceeding part (as if the function transitioned to a different shape; don't simply sum them)
Accuracy = (Dex * 2 + Vit * 1 + Int * 1)
Dodge = (Dex * 2 + Vit * 1 + Int * 1) * 0.25
Hit rate: 1.2 - opponent dodge / accuracy (= having 5x the accuracy of the dodge gives guaranteed hit rate)

XP growth formula: 5 * (level + 5) ^ 2.25

----

Players full heal after the completion of a dungeon's main objective and subsequent exiting. This allows limited MP to be used strategically without conservation; generally MP is not regenerated during a dungeon. Need to have some kind of penalty for exiting and re-entering, maybe dungeons have 10 min timers after entering before they can be re-entered?

----

The world map should be based on Finland.

The democracy of Finland has been plunged into chaos...
You must take up your weapons and save the land from monsters of myth and legend.

Savonlinna / Kuopio / Jyväskylä / Varkaus - small warrior / archer / magician / thief towns (1st job adv)
Turku / Tampere / Helsinki / Oulu - large warrior / archer / magician / thief cities (2nd job adv)

Other points of interest:

Kokkola
Mäntyharju
Lahti
Seinäjoki
Lappajärvi
Kuopio
Rovaniemi
Pori
Kotka
Ruka
Lieksa
Kajaani
Sodankylä
Utsjoki


----

World map based map generation, part 1. Each location has exits that start from the map closest to the location and end at the other point of interest. When travelling in the reverse direction, the maps are in reverse order as well, like a doubly linked list:

Mäntyharju - tiny NPC map, starting town for beginners at level 1
Exit 1 - east towards Savonlinna, 2 maps: forest (lv 1-4, 2 small rooms), forest (lv 4-7, 3 small rooms)

Savonlinna - medium NPC map, town/castle, allows job advancement to fighter 1st job
Exit 1: southwest towards Mäntyharju
Exit 2: northwest towards Varkaus, 1 map: plains (lv 7-9, 4 small rooms)
Exit 3: northeast towards Lieksa, 3 maps: lake (lv 10-12, 2 large rooms), forest (lv 12-16, 2 large rooms), deep forest (lv 16-21, 2 large rooms)

Varkaus - small NPC map, town, allows job advancement to rogue 1st job
Exit 1: south towards Savonlinna
Exit 2: north towards Kuopio, 1 map: lake (lv 9-12, 2 small rooms)
Exit 3: west towards Jyväskylä, 2 maps: forest (lv 8-10, 2 small rooms), plains (lv 10-14, 2 medium rooms)

Jyväskylä - large NPC map, town, allows job advancement to magician 1st job
Exit 1: east towards Varkaus
Exit 2: northeast towards Kuopio, 1 map: forest (lv 12-16, 2 medium rooms)

Kuopio - large NPC map, town, allows job advancement to archer 1st job
Exit 1: southwest towards Jyväskylä
Exit 2: south towards Varkaus
Exit 3: north towards Kajaani, 3 maps: lake (lv 12-16, 2 medium rooms), plains (lv 17-21, 2 large rooms), plains (lv 22-26, 2 large rooms)

Kajaani - medium NPC map, town, allows job advancement to 2nd job for all classes
Exit 1: south towards Kuopio
Exit 2: east towards Lieksa, 2 maps: forest (lv 18-22, 2 large rooms), deep forest (lv 23-27, 2 large rooms)

Lieksa - deep forest dungeon, 4 maps: deep forest (lv 20-24, 2 large rooms), deep forest (lv 25-29, 3 medium rooms), deep forest (lv 30-34, 5 small rooms), deep forest (lv 35-40, 2 large rooms)
Exit 1: north towards Kajaani
Exit 2: south towards Savonlinna

----

World map based map generation, part 2. Each location has exits that start from the map closest to the location and end at the other point of interest. When travelling in the reverse direction, the maps are in reverse order as well, like a doubly linked list:

(TODO based on world map; add cities all the way to Turku, then the eastern side until Rovaniemi)

----


The assets directory has assets for enemies. The assets have a semi-black cross going through them that should be ignored. Each asset file is typically 2048x2048, split into four quadrants:

q1 q2
q3 q4

Each quadrant is split into 16 tiles, each 256x256 pixels, similarly ordered and indexed 1-16.
Each enemy can be made of one or more adjacent tiles. Therefore the type for an enemy's asset tile data should have the type { filename: string, tiles: string | string[], facing?: 'left' | 'right' }, where filename is the path from within the assets subdirectory, and the tiles strings are in the format quadrant-index, for example "q1-12" or "q3-8". The facing tells whether the asset's face is facing to the left or to the right (right by default).

If an enemy is formed from two tiles, they are expected to be set up vertically like this

tile1
tile2

If from 4 tiles, they are in a grid like this:

tile1 tile2
tile3 tile4

Then we can start creating enemies that actually use these assets.

Typically enemy quadrants have the following structure:

1f1,1f2,1a1,1a2,2f1,2f2,2a1,2a2
1m1,1m2,1r1,1r2,2m1,2m2,2r1,2r2
1l1,1l1,1l2,1l2,2l1,2l1,2l2,2l2
1l1,1l1,1l2,1l2,2l1,2l1,2l2,2l2
3f1,3f2,3a1,3a2,4f1,4f2,4a1,4a2
3m1,3m2,3r1,3r2,4m1,4m2,4r1,4r2
3l1,3l1,3l2,3l2,4l1,4l1,4l2,4l2
3l1,3l1,3l2,3l2,4l1,4l1,4l2,4l2



The first character is the enemy race index, the second character is the class (fighter / archer / mage / rogue / leader), the third character is the tier (1 = normal (level 1-30), 2 = strong (level +8)). Typically fighter has the enemy's base level, archer +1, mage +4, rogue +2, and leader +12, but the exact numbers can shift +-3 depending on how a race specializes in things.

The enemy spritesheets have a black cross splitting the quadrants; it should be ignored.

In assets/forest-enemies.png, the enemy races are: 1 = menninkäinen, 2 = peikko, 3 = haltia, and 4 = metsänpeitto. 

In assets/aquatic-enemies.png, the enemy races are: 1 = Näkki, 2 = Vetehinen, 3 = Iku-Turso / Tursas, 4 = Vesihiisi

In assets/highway-enemies-3.png, the races are: 1 = unused, 2 = unused, 3 = Rosvo, 4 = fighter / archer / rogue are varas, others are kummitus / haamu

Add a mapping from these enemies to different biomes as appropriate, so that each biome has at least 2-3 possible enemy types. We'll add more types later.

----

Load multiple maps (rooms/zones); move tile data to JSON. Map connections/edges, per-map spawn tables, enter/exit transitions, world-state load/unload. Foundation for the world map. See world-map-design/ for the overworld.

Biomes (Finnish style): farmland / plains / forest / mythical misty forest / deep dark forest / lake / hill / mountain / tundra / snowy plains / snowy forest / cave / dungeon / village / town / city.

Each world-map location = one in-game map, with 1-8 connecting maps (=exits) containing monsters between them depending on distance (fewer in the south, more in the north).

Spawn tables need density + respawn params, e.g. maxAmount, spawnInterval (seconds), spawnAmount.

Procedural map shapes (a function/file taking params -> tile data). ~2-4 rooms per map, connected. Natural (caverns/forests): varied room shapes + twisting corridors. Human-made (settlements/dungeons): rectangular rooms + straight corridors; ensure all exits reachable. Aim for Diablo II / Brogue variety + emergent graphics.

Generation params: tileset, corridorWidth, roomCount (~2-4), roomSize (min/avg/max width), roomShape (rectangular / natural), torchDensity (per 100 wall tiles), torchGlowDistance, ambientLightLevel (0-100 opacity for un-torched areas), rooms as union of two rectangles, small/medium/large obstacles inside rooms, which of the 8 compass directions have portals and where.

Tile types: portal, floor, wall, 3x1 obstacle, 1x3 obstacle, 3x3 obstacle, 1x1 obstacle.

There are two asset files for maps in v1:

assets/tiles-floor.png
assets/tiles-obstacles.png

The floor is a repeatable floor tile in 4 biome quadrants:

forest,lake
deep-forest,town

Each quadrant is 512x512

The obstacles have similar layout. However, for each biome, they have the following sizes of obstacles:

1x1,3x1,3x1,3x1
1x3,3x3,3x3,3x3
1x3,3x3,3x3,3x3
1x3,3x3,3x3,3x3

(Adjacent tiles with the same size name are part of the same obstacle. Each tile is 128x128.)

----


List of classes:

Beginner

Fighter
Knight
Paladin
Duelist

Archer
Hunter
Sniper
Ranger

Magician
Arcanist
Wizard
Druid

Rogue
Assassin
Shadowblade
Reaver

----

The job advancement requirement levels are as follows:

Level 10 - 1st job
Level 30 - 2nd job
Level 60 - 1st job in a merged class
Level 80 - 2nd job in a merged class

Skills should have a maximum of 5 points for beginners, and 10 points for all other classes. On level up, the player gains 3 attribute points and 1 skill point. When starting the game, the player has just 1 point in the first beginner skill (a basic attack).

----

Enemies should allocate stats based on their class, configurable in config.ts:

fighter: 60% in str, 10% in vit, rest randomly
archer:  30% in str, 30% in dex, 8% in vit, rest randomly
magician: 60% in int, 6% in vit, rest randomly
rogue: 10% in str, 30% in dex, 30% in int, 7% in vit, rest randomly
leader: 20% vit, rest randomly

----

Class differentiation in terms of attack shapes:

Fighter: adjacent enemies, 4 tiles/8 tiles, 1x3 in front
Archer: long lines. Some of them cover the space right in front of them, others don't (especially for snipers).
Magician: 3x3, cones, distanced arcs, weird shapes like (1,0 + 3,-1 + 3,1)
Rogue: mix of all kinds of shapes; balanced thanks to lower minimum damage

There can also be differentiation of "costs" of skills within a single class:

MP cost, cooldown, number of uses before cooldown, delay until next skill, and range. For example, there might be a skill you can use as much as you have MP for, another with cooldown but no MP cost, and one with no costs but also limited range.

Generally:

Fighter: has one MP skill that is very strong for mobbing
Archer: can't hit right in front without using MP, or needs to "waste" turns pushing enemies into place
Magician: lots of cooldowns and trickier shapes to make up for high MP
Rogue: relatively _short_ delays and cooldowns to make them technical; also some MP costs

----

Damage multiplier guidelines (assuming max skill level):

0th job - single 1.4, multi 1.0, many 0.6
1st job - single 2.6, multi 1.6, many 1.1
2nd job - single 3.8, multi 2.2, many 1.6
3rd job - single 5.4, multi 3.2, many 2.1
4th job - single 8.0, multi 4.2, many 2.6

Cooldown and tricky skills: 1.5-1.8x or statuses

Elements by 2nd job class:

Warrior - earth / light / physical
Archer - air / physical / arcane
Magician - arcane / fire / earth
Rogue - physical / dark / ice

'physical' | 'light' | 'dark' | 'arcane' | 'fire' | 'ice' | 'air' | 'earth'

----

Fighter 1st job skills:

Power Strike - 1x1, 6 mp, lin(1.3, 0.15) damage
Power Stab - 2x1, uses: lin(2, 0.2), cooldown: 30s, lin(1.2, 0.12) damage
Spin Slash - 14 mp, surround (lin(4, 0.4) adjacent tiles), lin(0.9, 0.05) damage
Bracing Guard - reduce damage taken by lin(40, 4)% for 10s, cooldown: 30s

Archer 1st job skills:

Arrow Blow - 4x1 (non-piercing, hits closest), 6 mp, lin(1.1, 0.12) damage
Scatter Shot - 3-tile arc (offset 1), 16 mp, lin(1.0, 0.08) damage
Power Knockback - 1x1, knock back 3 tiles, uses: lin(2, 0.2), cooldown: 30s, lin(1.4, 0.12) damage
Improved Critical - passive, 45 mp, +lin(5, 2)% crit chance and +lin(10, 4)% crit damage for lin(100, 20)s

Magician 1st job skills:

Magic Claw - 3x1 (non-piercing), 12 mp, 2 hits, lin(0.6, 0.06) damage
Cross Blast - diagonal cross, 16 mp, uses: 4, cooldown: 30s, lin(1.4, 0.8) damage
Arcane Arc - lin(5, 0.2)-tile arc (offset 2), 22 mp, lin(1, 0.08) damage
Shocking Grasp - 1x1, 8 mp, slow lin(25, 5)%, uses: 2, cooldown: 30s, lin(1.3, 0.15) damage

Rogue 1st job skills:

Double Strike - 1x1, 6 mp, 2 hits, 1s trigger, lin(0.6, 0.04) damage
Venom Slash - 1x3 arc, poison lin(4, 0.4)% max HP every 2s over 10s, uses: 2, cooldown: 30s
Hamstring - 3x1 (non-piercing), 16 mp, slow lin(20, 3)%, lin(0.6, 0.06) damage
Life and Death - deal and take +lin(50, 5)% damage for 10s, cooldown: 30s

----

2nd job skills (merging will be planned later):

StatusKind → poison | bleed | burn | slow | stun | atkUp | atkDown | defUp | defDown | dodge | blind | statPct | statFlat

Fighter statuses: stun, long+short atk/def up / down

Knight - aggro, defense, atk/def buffs, easy to play
Backhand Bash, 18 mp, 1x3, stuns and deals lin(1, 0.15) damage
Sisu, 10% current hp, 1x1, deals damage equal to lin(1,0.2) times hp lost
Earth Aegis, 24 mp, 2x3, lin(0.8,0.1) damage and enemy atk/def down by lin(15,1)%
Muscle Up, passive +lin(2,2) str, max level 5
Bulk Up, passive +lin(2,2) vit, max level 5

Paladin - mob statuses (stun + blind), hybrid range (lightning), protection buffs
Lightning Hammer, 12 mp, 1x1, stun and lin(2.1,0.15) damage
Holy Flash, 16 mp, 3x3 blind for lin(25,5)%, cooldown 30s
Judgement Bolt, 20 mp, 5x1, stun and lin(1,0.1) damage
Holy Cross, 40 mp, 3x1 in adj-4 directions, deal lin(0.8,0.08) damage and heal allies (including user) for lin(10,1)% max HP

Duelist - melee 1v1, single target statuses, some ranged options
Riposte, 12 mp, 1x1, speed 3x, deals lin(2.4, 0.2) damage scaled by amount of time since last received attack (0% damage at 1 second). Guaranteed critical if last received attack was a miss
Lunge, 14 mp, dash forward up to 3 tiles, deal lin(1.2,0.08) damage +50% per tile dashed
Fatal Flurry, 22 mp, 1x1, deals lin(0.6,0.05) damage with hits equal to number of enemies in combat
Stance Change, instant (doesn't use turn/trigger), switch between a passive lin(5,1)% str or dex buff



Archer statuses: slow, some poison, some bleed, some blind, dodge, short dex up

Hunter - easy aoe damage, lower than mages but covers different areas more easily
Piercing Shot, 16 mp, 4 tile line, lin(1.4, 0.1) damage
Arrow Rain, 24 mp, 2x3 offset 1, lin(1,0.08) damage
Spinning Shot, 18 mp, 3 tiles in all 8 cardinal directions, each direction is separate non-piercing attack, lin(0.8, 0.08) damage 
Forest's Blessing, 24 mp, gain lin(5,1)% dex for 10s, cooldown 30s

Sniper - ranged 1v1, very long ranges at varied speeds
Snipeshot, 28 mp, 8x1, 0.4x speed, lin(4, 0.5) damage
Hurricane, 6 mp, 3x1 non-piercing, 3x speed, lin(0.75,0.05) damage
Pinpoint Rain, 22 mp, 1x1 offset 3, 0.8x speed, lin(4,0.2) hits, lin(0.7,0.02) damage
Forest's Mark, 24 mp, gain lin(5,2)% critical chance for 10s, cooldown 30s

Ranger - statuses and buffs, hybrid range
Poison Spray, 28 mp, 3 tiles in the three forward cardinal directions, each is a separate non-piercing attack, lin(3, 0.2)% maxhp poison damage per tick
Snare, 20 mp, 1x1 map tile effect, duration 30s, when stepped on by enemy causes stun, 50% slow, and lin(2.1, 0.15) damage, uses 4, cooldown 30s
Gash, 14 mp, 1x1, lin(1.6, 0.06) damage and bleed stack for lin(0.7, 0.07) total damage
Forest's Favor, 24 mp, gain lin(2,1) all stats for lin(100,20)s


Magician statuses: burn, stun, long dex up, short int up

Arcanist - tricky but powerful attacks and statuses; benefits from dex
Arcane Cataclysm, 44 mp, hits the 2 diagonals in front and the 3 tiles that can be moved straight forward to by chess knights from those diagonal tiles, lin(1.6, 0.1) dmg, speed 0.8x
Great Cross, 34 mp, 5 tile cross (splits from 3rd tile) which is straight every 2nd cast and diagonal every other cast, dmg lin(2.2, 0.1), uses: 4, cooldown: 30s
Chronostasis, 24 mp, deal lin(1.4, 0.08) damage and stun enemies in lin(3, 0.5) adjacent tiles, uses: lin(2,0.2), cooldown: 30s
Cat's Grace, 60 mp, gain +lin(10,2)% dex for lin(80,10)s

Wizard - easy aoe damage and burn statuses
Fireball, 24 mp, 3 tile range, hits first enemy, then explodes into 3x3. Damage lin(0.8,0.05), burns for lin(0.02, 0.003) every 0.5s
Firewall, 36 mp, 1x5, map tile effect, burns for lin(0.15, 0.01) every 0.5s, stays active lin(4,0.4)s, uses: 2, cooldown 30s
Burning Grasp 16 mp, 1x1, burns for lin(0.25,0.05) every 0.5s, cooldown: 30s
Mana Burn, 60 mp, gain +lin(10,2)% int for 10s, cooldown: 30s

Druid - crowd control and damage blocking; benefits from vit
Cone of Cold, 32 mp, 1/3/5 tile cone (range 3), slow 30%, lin(0.8,0.1) dmg, uses: lin(2,0.2), cooldown: 30s
Eruption, 88 mp, 5x5 centered on caster, lin(0.8, 0.1) damage, 0.6x speed
Raven Slash, 24 mp, diagonal 5-tile cross 2 tiles away, lin(1.2,0.1) damage
Natural Armor, 60 mp, gives the party temporary armor that blocks damage equal to lin(50, 10)% of user's max HP, cooldown lin(120, -6)


Rogue statuses: blind, poison, slow, bleed, short str/dex up, extra damage/crit vs statuses

Assassin - some crowd control, more likely criticals vs statuses

Poison Shot - 16 mp, 4 tile line (non-piercing), lin(0.6,0.02) damage, poison for lin(0.2,0.02) max hp / sec
Smoke Bomb - 22 mp, 2x3 in front, causes lin(0.1,0.03) blind
Gash Slash - 12 mp, 1x3, stack of bleed for lin(1.2,0.1) total damage
Coup de Grace, 40 mp, extra lin(5,1)% critical chance for each type of status effect on enemies

Shadowblade - easy class with improved stab/cleave, blind, temp. physical immunity; higher MP costs

Shadow Stab - 20 mp, 2 tile line, 2 hits, lin(0.8,0.04) damage
Shadow Cleave - 28 mp, 3 tile arc, 2 hits, lin(0.6,0.04) damage
Optic Slash - stab+cleave range, 2 hits, lin(1.1,0.06) damage, causes lin(0.2,0.04) blindness, cooldown 30s
Shadeform - 40 mp, lin(0.5,0.05) physical and lin(0.2,0.02) elemental damage resistance for 10s, cooldown 30s

Reaver - bleed stacking, dex buffs, fog, technical 1v1 specialist

Hack - 10 mp, 1x1, 2 hits, each deals lin(0.5, 0.03) damage and inflicts a stack of bleed for lin(0.4, 0.04) total damage
Eliminate - 18 mp, 1x1, removes all bleed stacks from enemy, deals 1 hit for each stack with lin(0.5,0.06) damage
Blizzard Blade - 24 mp, 4-adj spin, deals lin(0.8,0.08) dmg and inflicts a stack of bleed for lin(0.6,0.06) total damage
Forest Fangs - 36 mp, lin(0.1,0.03)% dex for 10s for each bleed stack on enemies in combat, cooldown 30s


----
